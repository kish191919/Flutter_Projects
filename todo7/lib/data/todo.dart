class Todo {
  int id;
  String title;
  String memo;
  String category;
  int color;
  int done;
  int date;

  Todo({this.id, this.title, this.memo, this.category, this.color, this.done, this.date});
}